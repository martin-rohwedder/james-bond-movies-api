spring:
  application:
    name: james-bond-movies-api
  jpa:
    hibernate:
      ddl-auto: create-drop #create-drop is only viable in a testing environment
  jackson:
    property-naming-strategy: SNAKE_CASE
